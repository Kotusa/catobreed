import styled from 'styled-components';

export const ListContainer = styled.ul`
  padding: 0;
  margin: 0;
  list-style: none;
  li {
    &:not(:last-child) {
      margin-bottom: 20px;
    }
  }
`;

export const ListItemConitaner = styled.li``;
